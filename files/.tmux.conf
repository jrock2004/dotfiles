set -g default-command "login-shell"

set -g history-limit 20000

# automatically renumber tmux windows
set -g renumber-windows on

# unbind default prefix and set it to Ctrl+a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Rather than constraining window size to the maximum size of any client
# connected to the *session*, constrain window size to the maximum size of any
# client connected to *that window*. Much more reasonable.
setw -g aggressive-resize on

# make delay shorter
set -sg escape-time 0

# make window/pane index start with 1
set -g base-index 1
setw -g pane-base-index 1

set-option -g set-titles on
set-option -g set-titles-string "#T - #W"

######################
#### Key Bindings ####
######################

# reload config file
bind r source-file ~/.tmux.conf \; display "Config Reloaded!"

# split window and fix path for tmux 1.9
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# synchronize all panes in a window
bind y setw synchronize-panes

# pane movement shortcuts
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# enable mouse support for switching panes/windows
setw -g mouse on

# theme stuff

## Status
set -g status-left ""
set -g status-right "♫ #(osascript -l JavaScript ~/.dotfiles/applescripts/tunes.js)"
set -g status-style "fg=#ffffff bg=#303030"

## Window
# setw -g window-status-format "#[fg=#eeeeef bg=#343843]#{?window_activity_flag,#[fg=#111111 bg=#cc575d],} #I #W #{?window_bell_flag,,}#{?window_content_flag,,}#{?window_silence_flag,,}#{?window_last_flag,,}"
# setw -g window-status-current-format "#[bg=#5294e2 fg=#ffffff bold] #I #W "

## Messaging
set -g message-style "fg=#ffffff bg=#5294e2 bright"


# ## Status
# set -g status-left ""

# ### Fancy Font
# set -g status-right "#{prefix_highlight} #[fg=#888888]#{pane_current_command}#[fg=#ffffff]  #S:#I.#P  %H:%M  #($HOME/.config/tmux/uptime.sh)  #{online_status}#[fg=#ffffff]  #($HOME/.config/tmux/cpu-load.sh) #[fg=#ffffff] #($HOME/.config/tmux/mem-usage.sh) "
# # setw -g pane-border-status bottom
# # setw -g pane-border-format ' #{pane_current_command} '
# ### For none fancy font replace icons with text or colour each segment differently
# set -g status-right-length 70
# set -g status-justify left
# set -g status-style "fg=#fffffe bg=#343843"
# ## Panes
# set -g pane-border-style "fg=#343843 bg=default"
# set -g pane-active-border-style "fg=#5294e2 bg=default"
# ## Window
# setw -g window-status-format "#[fg=#eeeeef bg=#343843]#{?window_activity_flag,#[fg=#111111 bg=#cc575d],} #I #W #{?window_bell_flag,,}#{?window_content_flag,,}#{?window_silence_flag,,}#{?window_last_flag,,}"
# setw -g window-status-current-format "#[bg=#5294e2 fg=#ffffff bold] #I #W "
# setw -g window-status-bell-style "fg=#111111 bg=#cc575d bold"
# setw -g window-status-activity-style "fg=#111111 bg=#cc575d bold"
# set -g window-status-separator '' # Remove space between windows in status bar
# ## Messaging
# set -g message-style "fg=#ffffff bg=#343843 bright"





# set -g status-justify "centre"
# set -g status "on"
# set -g status-left-style "none"
# set -g message-command-style "fg=#f8f8f2,bg=#5f6a8e"
# set -g status-right-style "none"
# set -g pane-active-border-style "fg=#bd93f9"
# set -g status-style "none,bg=#303030"
# set -g message-style "fg=#f8f8f2,bg=#ba8baf"
# set -g pane-border-style "fg=#5f6a8e"
# set -g status-right-length "100"
# set -g status-left-length "100"
# setw -g window-status-activity-style "none,fg=#bd93f9,bg=#303030"
# setw -g window-status-separator ""
# setw -g window-status-style "none,fg=#f8f8f2,bg=#303030"

# set -g status-left "#[fg=#282a36,bg=#bd93f9] #S #[fg=#bd93f9,bg=#5f6a8e,nobold,nounderscore,noitalics]#[fg=#f8f8f2,bg=#5f6a8e] #(osascript -l JavaScript ~/.dotfiles/applescripts/tunes.js) #[fg=#5f6a8e,bg=#303030,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=#bd93f9,bg=#303030,nobold,nounderscore,noitalics]#[fg=#282a36,bg=#bd93f9] %R  %a  %Y "
# setw -g window-status-format "#[fg=#303030,bg=#303030,nobold,nounderscore,noitalics]#[default] #I  #W #[fg=#303030,bg=#303030,nobold,nounderscore,noitalics]"
# setw -g window-status-current-format "#[fg=#303030,bg=#5f6a8e,nobold,nounderscore,noitalics]#[fg=#f8f8f2,bg=#5f6a8e] #I  #W #[fg=#5f6a8e,bg=#303030,nobold,nounderscore,noitalics]"
